// Module imports
import React from "react";

// Interface imports
import { constraintInterface } from "./Interfaces";

export default ({
    constraint
}: constraintInterface) => {

    // Code

    return (
        <span>
            { constraint }
        </span>
    );

};
